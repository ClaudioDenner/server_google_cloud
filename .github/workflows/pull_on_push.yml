name: Auto Push Server

on:
  push:
    branches:
      - main

jobs:
  pull:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Pull Changes
    - run: git pull origin main
    - run: echo "🖥️ The workflow is now ready to test your code on the runner."